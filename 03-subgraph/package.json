{
  "name": "pac",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ pac",
    "create-local": "graph create --node http://localhost:8020/ pac",
    "remove-local": "graph remove --node http://localhost:8020/ pac",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 pac"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "0.31.0"
  }
}